// src/types/index.ts

export type Job = {
    id: number;
    title: string;
    level: string;
    status: string;
    applicants: number;
    interviews: number;
    Experienced: string; // Assuming this is a string
    Respond: number;     // Assuming this is a number
    UnResponse: number;  // Assuming this is a number
    Datets: Date;        // Assuming this is a date
  };
  